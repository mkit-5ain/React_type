// history.push("/exp", { slideDirectionFrom: "right" });

// const navigate = useNavigate();
// navigate("/tickets/check", {
//   state: { slideDirectionFrom: "right" },
// });

const history = () => {};

export default history;
